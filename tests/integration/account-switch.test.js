describe('account switching', () => {
  it.todo('locks current signer when switching accounts');

  it.todo('prevents sending from a stale unlocked signer');

  it.todo('shows unlock screen for the newly selected account');
});

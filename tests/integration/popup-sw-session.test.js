describe('popup <-> service worker session', () => {
  it.todo('opens wallet screen only when the selected account is really unlocked');

  it.todo('redirects to unlock when service worker wallet is missing');

  it.todo('keeps session storage and service worker account state in sync');
});

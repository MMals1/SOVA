describe('smoke', () => {
  it('opens the popup without fatal errors', async () => {
    // TODO: open extension popup and assert main screen renders
  });

  it('allows switching between create and import tabs', async () => {
    // TODO: assert tab interaction works under MV3 CSP
  });
});

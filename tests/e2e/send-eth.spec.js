describe('ETH send flow', () => {
  it('shows confirm screen before send', async () => {
    // TODO: cover sendTransaction() confirm step
  });

  it('shows insufficient funds when signer balance is not enough', async () => {
    // TODO: cover insufficient funds case
  });

  it('submits ETH transfer from the selected unlocked account', async () => {
    // TODO: cover successful send path
  });
});

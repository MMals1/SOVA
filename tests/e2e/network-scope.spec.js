describe('network scope', () => {
  it('keeps transaction history isolated by network', async () => {
    // TODO: verify history cache separation by network
  });

  it('uses network-specific explorer links for transactions', async () => {
    // TODO: verify etherscan vs sepolia etherscan links
  });

  it('keeps token lists isolated by network', async () => {
    // TODO: verify mainnet/sepolia token separation
  });
});

describe('unlock flow', () => {
  it('unlocks an existing account with the correct password', async () => {
    // TODO: cover unlock path
  });

  it('shows an error for wrong password', async () => {
    // TODO: cover invalid password path
  });
});

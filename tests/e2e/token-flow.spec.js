describe('token flow', () => {
  it('adds token in current network only', async () => {
    // TODO: cover add token flow
  });

  it('renders token logo or fallback symbol', async () => {
    // TODO: cover token logo rendering
  });

  it('removes token from current network only', async () => {
    // TODO: cover remove token flow
  });
});
